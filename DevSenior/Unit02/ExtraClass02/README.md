# 🚗🏍️ Sistema de Gestión de Vehículos

## 📝 Descripción:

Este proyecto en Java permite gestionar una lista de vehículos, específicamente Carros y Motos. Implementa herencia para compartir propiedades comunes y métodos específicos para cada tipo.

### ✨ Características principales:

1.  **📊 Registro de Vehículos:** Permite ingresar marca, modelo y velocidad máxima.
2.  **🚗 Detalles de Carros:** Opción para registrar el número de puertas.
3.  **🏍️ Detalles de Motos:** Opción para indicar si la moto tiene sidecar.
4.  **🔄 Gestión Dinámica:** Uso de arreglos dinámicos para almacenar múltiples vehículos.

## 🧱 Estructura del Proyecto:

- `Vehiculo`: Clase base con atributos comunes (marca, modelo, velocidad).
- `Carro`: Extiende de `Vehiculo` y agrega el atributo `numPuertas`.
- `Moto`: Extiende de `Vehiculo` y agrega el atributo `tieneSideCar`.
- `Main`: Clase principal que maneja la interfaz de usuario por consola.

## 🛠️ Tecnologías Utilizadas:

- **Java** ☕: Lenguaje de programación.

---

# 🚗🏍️ Vehicle Management System

## 📝 Description:

This Java project allows managing a list of vehicles, specifically Cars and Motorcycles. It implements inheritance to share common properties and specific methods for each type.

### ✨ Key Features:

1.  **📊 Vehicle Registration:** Allows entering brand, model, and maximum speed.
2.  **🚗 Car Details:** Option to register the number of doors.
3.  **🏍️ Motorcycle Details:** Option to indicate if the motorcycle has a sidecar.
4.  **🔄 Dynamic Management:** Use of dynamic arrays to store multiple vehicles.

## 🧱 Project Structure:

- `Vehiculo`: Base class with common attributes (brand, model, speed).
- `Carro`: Extends `Vehiculo` and adds the `numPuertas` attribute.
- `Moto`: Extends `Vehiculo` and adds the `tieneSideCar` attribute.
- `Main`: Main class handling the console user interface.

## 🛠️ Technologies Used:

- **Java** ☕: Programming language.
